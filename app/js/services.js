(function() {
    'use strict';

    // Define the directive on the module.
    // Inject the dependencies. 
    // Point to the filter definition function.
    angular.module('myApp').service('services', ['$window', service]);
    
    function service ($window) {

    }

})();